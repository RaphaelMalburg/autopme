# Backend
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.8.0
pydantic-settings==2.4.0
python-multipart==0.0.9
python-dotenv==1.0.0
httpx==0.27.0
python-json-logger==2.0.7

# LiveKit agent + plugins
livekit-agents>=0.12.0
livekit-plugins-deepgram>=0.6.0
# STT local (faster-whisper) — alternativa sem chave
faster-whisper==1.0.3
# TTS local Piper via piper-tts (ou XTTS) — instalado separadamente conforme cenario
# piper-tts==1.2.0

# WhatsApp (Baileys e Node.js; aqui so o cliente HTTP para o gateway Node sidecar)
# O gateway Baileys corre como processo Node separado (ver app/whatsapp/)

# Email (Resend)
resend>=2.0.0

# Docs / PDF
pypdf==5.1.0

# Testes
pytest==8.3.0
pytest-asyncio==0.24.0
