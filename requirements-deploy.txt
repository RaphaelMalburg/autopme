# Dependencias slim do control-plane para deploy (Railway/Docker).
# Inclui so o necessario para: dashboard, cenarios, calculadora ROI,
# ingestao de documentos (OpenRouter vision + Resend) e mint de tokens
# LiveKit (livekit-api). NAO inclui deps pesadas de STT/TTS nem o worker
# livekit-agents (que corre localmente, nao na cloud).
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.8.0
pydantic-settings==2.4.0
python-multipart==0.0.9
python-dotenv==1.0.0
httpx==0.27.0
python-json-logger==2.0.7
resend>=2.0.0
pypdf==5.1.0
livekit-api>=0.8.0
